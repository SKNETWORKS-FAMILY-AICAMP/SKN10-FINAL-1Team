# PostgreSQL Connection String for direct database access
# Format: postgresql://username:password@host:port/database
POSTGRES_CONNECTION_STRING=postgresql://username:password@localhost:5432/sknetworks

# FastAPI Server URL for chat streaming
NEXT_PUBLIC_FASTAPI_URL=http://localhost:8001

# Set to 'true' to use mock data in development environment
NEXT_PUBLIC_USE_MOCK_DATA=false

# Django API URL (used for authentication and non-chat functionality)
NEXT_PUBLIC_API_URL=/api


DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=